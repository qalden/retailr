server:
  port: 8082

spring:
  application:
    name: retailr-catalog-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/retailr_db}
    username: ${SPRING_DATASOURCE_USERNAME:retailr_user}
    password: ${SPRING_DATASOURCE_PASSWORD:retailr_password}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  flyway:
    enabled: false

# Logging Configuration
logging:
  level:
    root: INFO
    com.retailr.catalog: DEBUG
    org.springframework.security: DEBUG

# Elastic APM Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      simple:
        enabled: true
